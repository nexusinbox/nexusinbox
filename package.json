{
  "name": "nexusinbox",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "pnpm": {
    "//": "Security floors for transitive deps flagged by `pnpm audit` / Dependabot (2026-06-12, updated 2026-09-04). Each pin is the advisory's patched version; all are semver-compatible with what the parents request (body-parser/qs sit under express 5 via @modelcontextprotocol/sdk, so >=2.3.0 / >=6.16.0 are patch bumps). extract-zip has no patched release (dev-only, via remotion/puppeteer in apps/promo-video).",
    "overrides": {
      "hono": ">=4.12.34",
      "@hono/node-server": ">=1.19.15",
      "fast-uri": ">=3.1.4",
      "ip-address": ">=10.3.1",
      "qs": ">=6.16.0",
      "body-parser": ">=2.3.0",
      "postcss": ">=8.5.12",
      "nanoid": ">=3.3.16",
      "ws": ">=8.21.0",
      "sharp": ">=0.35.0"
    }
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "test:contract": "vitest run tests/contract/openapi-contract.test.ts",
    "lint:contract": "redocly lint openapi/openapi.yaml",
    "contract:check": "pnpm lint:contract && pnpm test:contract",
    "lint": "turbo run lint",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "@apidevtools/json-schema-ref-parser": "^11.9.3",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "@redocly/cli": "^1.34.5",
    "@types/node": "^22.15.0",
    "@typescript-eslint/eslint-plugin": "^8.31.0",
    "@typescript-eslint/parser": "^8.31.0",
    "eslint": "^9.25.0",
    "eslint-config-prettier": "^10.1.2",
    "prettier": "^3.5.3",
    "turbo": "^2.5.1",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
