{
  "name": "@nexusinbox/api-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build",
    "test": "cargo test",
    "lint": "cargo fmt --check && cargo clippy -- -D warnings"
  }
}
